---
label: Page
hide_body: true
fields:
- name: name
  type: text
  config:
    required: false
  label: Name
- name: works
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: works
  label: Works
pages:
- data/pages/home.yml
